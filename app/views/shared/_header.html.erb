<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <div class-"navbar-header">
      <a class="navbar-brand" href= <%= root_url %> >RunLogger</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if signed_in? %>
        <ul class= "nav navbar-nav">
            <li><%= link_to t(:profile), user_path(current_user) %></li>
            <li><%= link_to t(:logs), "/users/#{current_user.id}/log/0" %></li>
            <li><%= link_to t(:races), user_races_path(current_user) %></li>
            <li><%= link_to t(:pbs), "/pb/#{current_user.id}" %></li>
            <li><%= link_to t(:phases), "/users/#{current_user.id}/phases/" %></li>
            <li><%= link_to t(:csv), "/log_import/" %></li>
            <li class="divider"><li>
        </ul>
        <ul class="nav navbar-nav pull-right">
          <li><%= link_to t(:signout), signout_path, method: "delete" %></li>
        </ul>
      <% end %>

      <% if !signed_in? %>
        <ul class= "nav navbar-nav pull-right">
          <li><%= link_to t(:signin), signin_path %></li>
          <li><%= link_to t(:signup), signup_path %></li>
        </ul>
      <% end %>
    </div>  
  </div>
</nav>