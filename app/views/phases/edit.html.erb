<div id="container1">
  <h1>Edit phased training period.</h1>
  <p>When choosing a period for a training phase, it will default to a start_date of the next available Monday, and a
     target date of the next available Sunday from the dates selected. Training periods cannot overlap.
  </p>
  <%= render 'shared/error_messages', object: @phase %>
  <%= form_for [@user,@phase] do |f| %>
    <p>
      <%= f.label :start_date %>
      <%= f.date_field :start_date %>
    </p>
    <p>
      <%= f.label :target_date %>
      <%= f.date_field :target_date %>
    </p>
    <p>
      <%= f.label "Include phase I" %>
      <%= f.check_box :include_phase_I %>
    </p>

    <%= f.submit "Submit", class: "btn btn-lg btn-custom" %> 
  <% end %>
</div>